// This class represents the data that is being sent between the client and the server. 
// It implements the Serializable interface, which means its objects can be (de)converted to a byte stream and sent over a network connection, saved to a file, etc.

public class Message implements Serializable {
    // ... <-- anything that is also serializable (see files)
}